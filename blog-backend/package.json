{
  "name": "bloglistnode",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "build:ui": "rm -rf build && cd ../blog-frontend/bloglist-frontend && npm run build --prod && cp -r build ../blog-backend/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy",
    "logs:prod": "heroku logs --tail",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand",
    "start:test": "cross-env NODE_ENV=test node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^8.6.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.27",
    "mongoose-unique-validator": "^2.0.3"
  },
  "devDependencies": {
    "@swc/core": "^1.2.62",
    "@swc/wasm": "^1.2.62",
    "bufferutil": "^4.0.1",
    "canvas": "^2.5.0",
    "cross-env": "^7.0.2",
    "jest": "^26.2.2",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.4",
    "utf-8-validate": "^5.0.2"
  },
  "engines": {
    "node": "12.x",
    "npm": "7.18"
  }
}
